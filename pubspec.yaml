name: mini_katalog
description: Flutter Mini Katalog Uygulaması - Eğitim Projesi
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Banner görseli assets klasöründen yükleniyor
  assets:
    - assets/banner.png